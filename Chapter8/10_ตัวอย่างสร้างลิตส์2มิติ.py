A = [[1, 2, 3],
    [4, 5, 6]]  # สร้างลิสต์ 2 มิติขนาด 2×3

for r in range(len(A)):        # วนลูปแต่ละแถว (row)
    for c in range(len(A[0])): # วนลูปแต่ละคอลัมน์ (column)
        print(A[r][c])         # แสดงค่าของแต่ละช่อง